# 🛋️ پروژه بک‌اند فروشگاه مبلمان (Furniture Store API)
> **هدف:** ساخت API واقعی، امن و استاندارد با استفاده از Node.js، Express و MongoDB

---

## 📌 فاز ۱: راه‌اندازی زیربنا و ساختار اولیه پروژه (Setup)
* [ ] **تسک ۱-۱:** ساخت پوشه پروژه (`furniture-shop`) و اجرای دستور `npm init -y`
* [ ] **تسک ۱-۲:** نصب وابستگی‌های اصلی (`npm i express mongoose dotenv bcryptjs jsonwebtoken nodemailer`)
* [ ] **تسک ۱-۳:** نصب وابستگی‌های محیط توسعه (`npm i -D nodemon`)
* [ ] **تسک ۱-۴:** پیکربندی `package.json` و اضافه کردن اسکریپت `"dev": "nodemon server.js"`
* [ ] **تسک ۱-۵:** ساخت فایل `.env` و تنظیم متغیرهای `PORT` و `DATABASE_URI`
* [ ] **تسک ۱-۶:** ساخت فایل `server.js` و راه‌اندازی سرور پایه Express
* [ ] **تسک ۱-۷:** ایجاد اتصال به دیتابیس MongoDB با Mongoose
* [ ] **تسک ۱-۸:** ساخت پوشه‌بندی استاندارد MVC شامل (`controllers`, `models`, `routes`, `utils`, `middleware`)
* [ ] **تسک ۱-۹:** تست اولیه سرور با ارسال یک درخواست ساده در Postman

---

## 📌 فاز ۲: مدیریت محصولات و فیلتر پیشرفته (Products & Filtering)
* [ ] **تسک ۲-۱:** طراحی اسکیمای محصول (`models/productModel.js`) شامل فیلدهای `name`, `price`, `capacity`, `color`, `category`, `imageCover`, `description`
* [ ] **تسک ۲-۲:** ساخت روت و کنترلر برای ایجاد محصول جدید (`POST /api/v1/products`)
* [ ] **تسک ۲-۳:** ساخت روت و کنترلر برای دریافت یک محصول با آیدی (`GET /api/v1/products/:id`)
* [ ] **تسک ۲-۴:** ساخت روت برای دریافت همه محصولات (`GET /api/v1/products`)
* [ ] **تسک ۲-۵:** پیاده‌سازی منطق فیلتر بر اساس `req.query` (مثلاً فیلتر ظرفیت `capacity` و رنگ `color`)
* [ ] **تسک ۲-۶:** اضافه کردن قابلیت مرتب‌سازی (Sorting) و محدودسازی فیلدها (Field Limiting)
* [ ] **تسک ۲-۷:** پیاده‌سازی صفحه‌بندی (Pagination) در دریافت لیست محصولات
* [ ] **تسک ۲-۸:** وارد کردن ۵ تا ۱۰ محصول نمونه (مبل ۳ نفره، ۲ نفره، تک‌نفره) و تست کامل فیلترها در Postman

---

## 📌 فاز ۳: کاربران، امنیت و احراز هویت (User Auth & Security)
* [ ] **تسک ۳-۱:** طراحی اسکیمای کاربر (`models/userModel.js`) شامل `name`, `email`, `password`, `role`
* [ ] **تسک ۳-۲:** اضافه کردن میدل‌ور پیش از ذخیره‌سازی (Pre-save hook) برای هش کردن پسورد با `bcryptjs`
* [ ] **تسک ۳-۳:** پیاده‌سازی متد مقایسه پسورد (Password verification) در مدل کاربر
* [ ] **تسک ۳-۴:** ساخت ابزار کمکی تولید توکن JWT در `utils/generateToken.js`
* [ ] **تسک ۳-۵:** ساخت کنترلر ثبت‌نام (`POST /api/v1/users/signup`) و صدور توکن
* [ ] **تسک ۳-۶:** ساخت کنترلر ورود (`POST /api/v1/users/login`) و صحت‌سنجی اطلاعات
* [ ] **تسک ۳-۷:** ساخت میدل‌ور محافظت از روت‌ها (`middleware/authMiddleware.js`) برای بررسی توکن JWT
* [ ] **تسک ۳-۸:** ساخت میدل‌ور محدودسازی دسترسی بر اساس نقش کاربر (مثلاً فقط ادمین بتواند محصول ایجاد کند)

---

## 📌 فاز ۴: سبد خرید، سفارشات و سرویس ایمیل (Orders & Emailing)
* [ ] **تسک ۴-۱:** کانفیگ سرویس ارسال ایمیل با `nodemailer` در `utils/email.js`
* [ ] **تسک ۴-۲:** ارسال ایمیل خوش‌آمدگویی خودکار پس از ثبت‌نام موفق کاربر
* [ ] **تسک ۴-۳:** طراحی اسکیمای سفارش (`models/orderModel.js`) ارتباط با کاربر و محصولات
* [ ] **تسک ۴-۴:** ساخت کنترلر ایجاد سفارش جدید (`POST /api/v1/orders`) و محاسبه قیمت کل
* [ ] **تسک ۴-۵:** ساخت کنترلر مشاهده سفارشات یک کاربر (`GET /api/v1/orders/my-orders`)
* [ ] **تسک ۴-۶:** ارسال ایمیل فاکتور و تایید سفارش پس از ثبت موفق خریدهای کاربر

---

## 📌 فاز ۵: مدیریت خطا، پایداری و نهایی‌سازی (Error Handling & Polish)
* [ ] **تسک ۵-۱:** ساخت کلاس سفارشی مدیریت خطا (`utils/appError.js`)
* [ ] **تسک ۵-۲:** ساخت میدل‌ور مرکزی مدیریت خطاهای Express (`controllers/errorController.js`)
* [ ] **تسک ۵-۳:** هندل کردن خطاهای رایج MongoDB (مثل آیدی اشتباه یا ایمیل تکراری)
* [ ] **تسک ۵-۴:** بررسی و تمیزکاری کدهای پروژه، حذف console.logهای اضافی
* [ ] **تسک ۵-۵:** مستندسازی APIها و آماده‌سازی فایل تست کامل برای Postman
